# This is where table data is established
# Use for both teacher and student
# Example:
# class Student(Base):
        # id = Integer(Integer, primary_key=True)
        # first_name = Column(String[50])
